# spa-react-shadcn-tanstack

Template for a Single Page Application (SPA) using React, Shadcn/ui, and TanStack Router. Intended to be used as a starting point — another LLM or developer can scaffold this template and have a production-ready structure from which end users can begin building immediately.

---

## Stack

| Layer | Technology |
|---|---|
| Framework | React 19 |
| Build tool | Vite |
| Language | TypeScript |
| Router | TanStack Router v1 (file-based routing) |
| UI components | Shadcn/ui |
| Styling | Tailwind CSS v4 |
| Package manager | pnpm |

---

## Minimum requirements

### Dependencies

```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@tanstack/react-router": "^1.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "@tanstack/router-plugin": "^1.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0"
  }
}
```

Shadcn/ui is installed component-by-component via its CLI — it is not a single npm package. The minimum Shadcn setup requires:

```json
{
  "dependencies": {
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0",
    "lucide-react": "^0.400.0",
    "@radix-ui/react-slot": "^1.0.0"
  }
}
```

---

## Project structure

```
/
├── public/
│   └── favicon.ico
├── src/
│   ├── components/
│   │   └── ui/                  # Shadcn components live here (auto-generated by CLI)
│   ├── routes/
│   │   ├── __root.tsx           # Root layout (TanStack Router)
│   │   └── index.tsx            # Home route "/"
│   ├── lib/
│   │   └── utils.ts             # cn() helper required by Shadcn
│   ├── main.tsx                 # App entry point
│   └── routeTree.gen.ts         # Auto-generated by TanStack Router plugin (do not edit)
├── components.json              # Shadcn/ui configuration file
├── index.html
├── tsconfig.json
├── tsconfig.app.json
├── vite.config.ts
└── package.json
```

---

## Key configuration files

### `vite.config.ts`

Must include the TanStack Router Vite plugin (for file-based routing) and the Tailwind CSS Vite plugin:

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { TanStackRouterVite } from '@tanstack/router-plugin/vite'
import tailwindcss from '@tailwindcss/vite'
import path from 'path'

export default defineConfig({
  plugins: [
    TanStackRouterVite({ target: 'react', autoCodeSplitting: true }),
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

### `tsconfig.app.json`

Must include the `@` path alias:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

### `components.json`

Shadcn configuration. Generated by `pnpm dlx shadcn@latest init`:

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

### `src/lib/utils.ts`

Required by every Shadcn component:

```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

### `src/main.tsx`

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { RouterProvider, createRouter } from '@tanstack/react-router'
import { routeTree } from './routeTree.gen'
import './index.css'

const router = createRouter({ routeTree })

declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router
  }
}

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <RouterProvider router={router} />
  </StrictMode>,
)
```

### `src/routes/__root.tsx`

Root layout that wraps all routes:

```tsx
import { createRootRoute, Outlet } from '@tanstack/react-router'

export const Route = createRootRoute({
  component: () => (
    <main className="min-h-screen">
      <Outlet />
    </main>
  ),
})
```

### `src/routes/index.tsx`

Home route at `/`:

```tsx
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/')({
  component: Index,
})

function Index() {
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold">Home</h1>
    </div>
  )
}
```

---

## Scaffold steps (for an LLM agent)

These are the ordered steps to bring this template to a working state from scratch:

1. `pnpm create vite . --template react-ts` — initialize Vite + React + TypeScript
2. Generate a complete `.gitignore` covering: `node_modules/`, `dist/`, `.vite/`, `*.tsbuildinfo`, `node_modules/.tmp/`, `.env*`, `.pnpm-store/`, editor dirs (`.vscode/`, `.idea/`), OS files (`.DS_Store`, `Thumbs.db`), and test/coverage output. Leave `src/routeTree.gen.ts` uncommented so the repo works without requiring a dev run.
3. `pnpm install` — install base dependencies
4. Install TanStack Router: `pnpm add @tanstack/react-router` and `pnpm add -D @tanstack/router-plugin`
5. Install Tailwind CSS v4: `pnpm add -D tailwindcss @tailwindcss/vite`
6. Add `@import "tailwindcss";` to `src/index.css` (replaces old tailwind directives)
7. Update `vite.config.ts` with the plugins and `@` alias shown above
8. Update `tsconfig.app.json` with the `@` path alias
9. Initialize Shadcn: `pnpm dlx shadcn@latest init` (choose defaults, `neutral` base color, CSS variables enabled)
10. Replace `src/main.tsx` with the TanStack Router setup shown above
11. Create `src/routes/__root.tsx` and `src/routes/index.tsx`
12. Run `pnpm dev` once so TanStack Router generates `src/routeTree.gen.ts`
13. Optionally add a first Shadcn component to verify the stack works: `pnpm dlx shadcn@latest add button`

---

## Adding routes

Create a file under `src/routes/`. TanStack Router will auto-generate the route tree on the next `pnpm dev` or `pnpm build`.

```
src/routes/about.tsx        →  /about
src/routes/users/index.tsx  →  /users
src/routes/users/$id.tsx    →  /users/:id
```

## Adding Shadcn components

```bash
pnpm dlx shadcn@latest add <component-name>
```

Components are added to `src/components/ui/` and can be imported as `@/components/ui/<component>`.

---

## Scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  }
}
```
